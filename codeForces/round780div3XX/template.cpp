#include <iostream>
#include <vector>
using namespace std;

void testCase()
{


}

int main()
{
    ios_base::sync_with_stdio(false); 
    cin.tie(0);
    cout.tie(0);

    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        testCase();
    }

    return 0;
}